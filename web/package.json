{
  "name": "wisecortex-web",
  "private": true,
  "version": "0.9.19",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/clean-dist.mjs && tsc --noEmit && vite build",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "dependencies": {
    "highlight.js": "^11.10.0",
    "marked": "^14.1.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
